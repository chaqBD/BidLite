# Streamlit Community Cloud — Secrets
# Paste the contents below (without the .example extension) into:
#   share.streamlit.io → your app → Settings → Secrets
#
# Do NOT commit the real secrets.toml — it is in .gitignore

OPENAI_API_KEY    = "sk-proj-..."
QDRANT_URL        = "https://xxxx.aws.cloud.qdrant.io"
QDRANT_API_KEY    = "eyJhbG..."
QDRANT_COLLECTION = "bidlite"
